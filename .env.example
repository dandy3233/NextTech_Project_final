# Environment Variables — NextTech Project

# Backend server URL (required for production builds)
# In development, Vite proxies /api and /Public automatically
# VITE_BACKEND_URL=http://your-backend-server:8000
